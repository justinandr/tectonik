import { Text, View } from 'react-native';

export default function NewTicketScreen() {
  return (
    <View
      style={{
        flex: 1,
        justifyContent: "center",
        alignItems: "center"
      }}
    >
      <Text>New Ticket Screen</Text>
    </View>
  );
}
